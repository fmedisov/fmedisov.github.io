apiVersion: v1
kind: Service
metadata:
  name: {{ include "mse-chart.fullname" . }}
  labels:
    {{- include "mse-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mse-chart.selectorLabels" . | nindent 4 }}
